[project]
name = "ipyquiz"
version = "0.1.0"
description = "Quizzes for Jupyter"
dependencies = [
    "ipython",
    "ipywidgets",
]


[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ipyquiz"]